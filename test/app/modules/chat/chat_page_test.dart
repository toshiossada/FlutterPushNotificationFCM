import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:push_notification/app/modules/chat/chat_page.dart';

void main() {
  testWidgets('ChatPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ChatPage(title: 'Chat')));
    //  final titleFinder = find.text('Chat');
    //  expect(titleFinder, findsOneWidget);
  });
}
