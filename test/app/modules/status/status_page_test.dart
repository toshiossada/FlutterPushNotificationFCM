import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:push_notification/app/modules/status/status_page.dart';

void main() {
  testWidgets('StatusPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(StatusPage(title: 'Status')));
    //  final titleFinder = find.text('Status');
    //  expect(titleFinder, findsOneWidget);
  });
}
